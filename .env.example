# Optional: enables LLM-powered agent responses (Anthropic)
# Without this key, the agent still works using local tools + search
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
AGENT_PORT=8787
